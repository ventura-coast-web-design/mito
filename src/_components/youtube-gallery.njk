<section id="videos" class="section videos" aria-labelledby="videos-title">
  <div class="container">
    <header class="section__head">
      <h2 id="videos-title">Jumpstart your understanding:</h2>
      <p>
        While we wait for the presentations to start, get a taste of what we’ll be covering.
      </p>
    </header>

    <div class="videos__grid">
      {% for v in videos %}
        <article class="video-card video-card--teaser">
          <a class="video-card__teaser-link" href="/videos/{{ v.slug }}/">
            <div class="video-card__thumb">
              <img
                src="https://img.youtube.com/vi/{{ v.youtubeId }}/hqdefault.jpg"
                alt=""
                width="480"
                height="360"
                loading="lazy"
                decoding="async"
              />
              <span class="video-card__play" aria-hidden="true">Play</span>
            </div>
            <p class="video-card__title">{{ v.title }}</p>
          </a>
          {% if v.transcript != false %}
            {% set transcriptSlug = v.transcriptSlug | default(v.slug) %}
            <p class="video-card__transcript-wrap">
              <a class="video-card__transcript" href="/transcripts/{{ transcriptSlug }}/">Transcript</a>
            </p>
          {% endif %}
        </article>
      {% endfor %}
    </div>

    <p class="videos__cta">
      <a class="btn btn--primary" href="/videos/">Video hub &amp; full list</a>
    </p>
  </div>
</section>
